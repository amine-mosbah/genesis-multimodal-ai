apiVersion: apps/v1
kind: Deployment
metadata:
  name: cyclegan-worker-deployment
  namespace: multimodal-ai
  labels:
    app: cyclegan-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cyclegan-worker
  template:
    metadata:
      labels:
        app: cyclegan-worker
    spec:
      containers:
      - name: cyclegan-worker
        image: multimodal-cyclegan-worker:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8005
        envFrom:
        - configMapRef:
            name: app-config
        env:
        - name: HUGGINGFACE_API_KEY
          valueFrom:
            secretKeyRef:
              name: api-keys
              key: huggingface-api-key
        volumeMounts:
        - name: storage
          mountPath: /data
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
      volumes:
      - name: storage
        persistentVolumeClaim:
          claimName: multimodal-storage-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: cyclegan-worker-service
  namespace: multimodal-ai
spec:
  selector:
    app: cyclegan-worker
  ports:
  - protocol: TCP
    port: 8005
    targetPort: 8005
  type: ClusterIP
